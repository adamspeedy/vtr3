# vtr_db_extractor

Extracts data stored in the sqlite bag file format for analysis. 

- First extract necessary data eg.
```
ros2 run vtr_db_extractor graph_loc_offset_extractor 
```

(note. Make sure to double check the file paths in the src files used)
